# Simple-Task-Manager-API
This project implements a RESTful API for managing tasks, built with ASP.NET Core Minimal APIs. It supports basic CRUD operations (Create, Read, Update, Delete) while following RESTful best practices.
